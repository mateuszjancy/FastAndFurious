fnf-db = {
  driver = "com.mysql.cj.jdbc.Driver"
  url = "jdbc:mysql://127.0.0.1:3306/fnf?serverTimezone=UTC"
  url = ${?MYSQL_URL}
  user = "root"
  user = ${?MYSQL_USER}
  password = ""
  password = ${?MYSQL_PASSWORD}
  connectionPool = disabled
}

apikey = "[TBP]"
apikey = ${?APIKEY}

bootstrap = false
bootstrap = ${?BOOTSTRAP}
